const currentUserReducer = (state = null, action) => {
  switch (action.type) {
    // case label_1:
    //   // statements_1
    //   break;
    default:
      return state;
  }
}

export default currentUserReducer;